<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin | Métodos de Pagamento</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body { font-family: 'Inter', sans-serif; background: #f1f5f9; }
    .sidebar { min-height: 100vh; background: linear-gradient(180deg, #0f172a 0%, #1e1b4b 100%); color: white; position: sticky; top: 0; height: 100vh; }
    .sidebar a { color: #94a3b8; text-decoration: none; padding: 12px 20px; display: block; border-radius: 12px; margin: 4px 10px; transition: 0.3s; }
    .sidebar a:hover, .sidebar a.active { background: rgba(255,255,255,0.15); color: white; transform: translateX(5px); border-left: 4px solid #22d3ee; }
    .admin-card { border: none; border-radius: 20px; background: white; box-shadow: 0 8px 24px rgba(0,0,0,0.04); }
    .action-btn { width: 35px; height: 35px; border-radius: 8px; display: inline-flex; align-items: center; justify-content: center; transition: 0.2s; }
    .action-btn:hover { transform: scale(1.1); }
    .modal-content { border-radius: 20px; }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-3 col-lg-2 sidebar p-3 d-flex flex-column">
        <div class="sidebar-header text-center mb-4">
          <h4 class="fw-bold text-warning mt-2"><i class="fa-solid fa-screwdriver-wrench me-2"></i> Admin</h4>
          <hr class="border-secondary">
          <div class="text-start small text-light-emphasis mt-2 px-2"><i class="fa-regular fa-circle-user me-2"></i> <%= user ? user.username : 'Admin' %></div>
        </div>
        <nav class="nav flex-column mb-auto">
          <a href="/admin/dashboard"><i class="fa-solid fa-chart-simple me-2"></i> Dashboard</a>
          <a href="/admin"><i class="fa-solid fa-box me-2"></i> Produtos</a>
          <a href="/admin/users"><i class="fa-solid fa-users me-2"></i> Utilizadores</a>
          <a href="/admin/discounts"><i class="fa-solid fa-tags me-2"></i> Descontos</a>
          <a href="/admin/payment-methods" class="active"><i class="fa-solid fa-money-bill-transfer me-2"></i> Pagamentos</a>
          <a href="/admin/logs"><i class="fa-solid fa-clock-rotate-left me-2"></i> Logs</a>
          <a href="/admin/settings"><i class="fa-solid fa-gear me-2"></i> Configurações</a>
        </nav>
        <div class="mt-auto pt-3 border-top border-secondary">
          <a href="/" class="text-light"><i class="fa-solid fa-arrow-left me-2"></i> Voltar ao Site</a>
          <a href="/logout" class="text-danger mt-2"><i class="fa-solid fa-right-from-bracket me-2"></i> Sair</a>
        </div>
      </div>

      <div class="col-md-9 col-lg-10 p-4">
        <div class="admin-card shadow-sm mb-4 p-4">
          <h5 class="fw-bold mb-4"><i class="fa-solid fa-plus-circle text-success me-2"></i> Adicionar Método de Pagamento</h5>
          <form action="/admin/payment-methods/add" method="POST" class="row g-3">
            <div class="col-md-4">
              <label class="form-label small fw-bold text-muted">Nome</label>
              <input type="text" name="name" class="form-control bg-light border-0" placeholder="Ex: MB WAY" required>
            </div>
            <div class="col-md-4">
              <label class="form-label small fw-bold text-muted">Descrição</label>
              <input type="text" name="description" class="form-control bg-light border-0" placeholder="Descrição curta">
            </div>
            <div class="col-md-2">
              <label class="form-label small fw-bold text-muted">Ícone</label>
              <input type="text" name="icon" class="form-control bg-light border-0" placeholder="fa-mobile-screen">
            </div>
            <div class="col-md-2">
              <label class="form-label small fw-bold text-muted">Ordem</label>
              <input type="number" name="display_order" class="form-control bg-light border-0" value="0">
            </div>
            <div class="col-12">
              <button type="submit" class="btn btn-primary fw-bold shadow-sm"><i class="fa-solid fa-check me-2"></i> Adicionar Método</button>
            </div>
          </form>
        </div>

        <div class="admin-card shadow-sm">
          <div class="p-4 border-bottom">
            <h5 class="fw-bold mb-0"><i class="fa-solid fa-money-bill-transfer text-primary me-2"></i> Métodos de Pagamento Existentes</h5>
          </div>
          <div class="p-0">
            <% if (error) { %><div class="alert alert-danger m-4"><%= error %></div><% } %>
            <div class="table-responsive">
              <table class="table table-hover align-middle mb-0">
                <thead class="bg-light">
                  <tr><th class="ps-4">ID</th><th>Nome</th><th>Descrição</th><th>Ícone</th><th>Ordem</th><th>Ativo</th><th class="text-end pe-4">Ações</th></tr>
                </thead>
                <tbody>
                  <% methods.forEach(m => { %>
                    <tr>
                      <td class="ps-4 fw-bold text-secondary">#<%= m.id %></td>
                      <td class="fw-semibold"><%= m.name %></td>
                      <td class="small text-muted"><%= m.description %></td>
                      <td><i class="fa-solid <%= m.icon || 'fa-credit-card' %>"></i></td>
                      <td><%= m.display_order %></td>
                      <td>
                        <% if (m.is_active === 1) { %><span class="badge bg-success rounded-pill px-3">Ativo</span><% } else { %><span class="badge bg-secondary rounded-pill px-3">Inativo</span><% } %>
                      </td>
                      <td class="text-end pe-4">
                        <div class="d-flex justify-content-end gap-2">
                          <button type="button" class="action-btn btn btn-warning" title="Editar" data-bs-toggle="modal" data-bs-target="#editModal<%= m.id %>"><i class="fa-solid fa-pen-to-square"></i></button>
                          <form action="/admin/payment-methods/toggle/<%= m.id %>" method="POST" class="d-inline">
                            <button type="submit" class="action-btn <%= m.is_active === 1 ? 'btn btn-outline-danger' : 'btn btn-success' %>" title="<%= m.is_active === 1 ? 'Desativar' : 'Ativar' %>">
                              <% if (m.is_active === 1) { %><i class="fa-solid fa-eye-slash"></i><% } else { %><i class="fa-solid fa-eye"></i><% } %>
                            </button>
                          </form>
                          <form action="/admin/payment-methods/delete/<%= m.id %>" method="POST" class="d-inline">
                            <button type="submit" class="action-btn btn btn-danger" title="Apagar" onclick="return confirm('Tem a certeza que deseja apagar este método?');"><i class="fa-solid fa-trash-can"></i></button>
                          </form>
                        </div>
                      </td>
                    </tr>

                    <!-- Modal de Edição -->
                    <div class="modal fade" id="editModal<%= m.id %>" tabindex="-1">
                      <div class="modal-dialog modal-dialog-centered">
                        <div class="modal-content p-2">
                          <div class="modal-header border-0">
                            <h5 class="modal-title fw-bold">Editar: <%= m.name %></h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                          </div>
                          <form action="/admin/payment-methods/edit/<%= m.id %>" method="POST">
                            <div class="modal-body">
                              <div class="mb-3"><label class="form-label small">Nome</label><input type="text" name="name" class="form-control" value="<%= m.name %>" required></div>
                              <div class="mb-3"><label class="form-label small">Descrição</label><input type="text" name="description" class="form-control" value="<%= m.description %>"></div>
                              <div class="mb-3"><label class="form-label small">Ícone</label><input type="text" name="icon" class="form-control" value="<%= m.icon %>"></div>
                              <div class="mb-3"><label class="form-label small">Ordem</label><input type="number" name="display_order" class="form-control" value="<%= m.display_order %>"></div>
                            </div>
                            <div class="modal-footer border-0">
                              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                              <button type="submit" class="btn btn-primary fw-bold">Guardar</button>
                            </div>
                          </form>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>